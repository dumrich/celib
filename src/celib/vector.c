#include "vector.h"
#include <stdlib.h>

Vec* vector_init(size_t size) {
    Vec* v = malloc(sizeof(Vec)); 
    v->s
}

void vector_add(void*);
void* vector_remove(void*);

void* vector_get(int);

void vector_free(Vec*);